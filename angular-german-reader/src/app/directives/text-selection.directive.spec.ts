import { TextSelectionDirective } from './text-selection.directive';

describe('TextSelectionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
